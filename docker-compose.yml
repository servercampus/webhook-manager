version: "3.9"

services:
  webhook-manager:
    container_name: servercampus-webhook-manager
    build: .
    ports:
      - "8080:8080"
    environment:
      - FLASK_RUN_PORT=8080
      - FLASK_DEBUG=0
      - FLASK_SECRET_KEY=${FLASK_SECRET_KEY:-change-me}
    restart: unless-stopped
